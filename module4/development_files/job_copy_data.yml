apiVersion: batch/v1
kind: Job
metadata:
  name: job-data-download-sso
spec:
  template:
    spec:
      automountServiceAccountToken: false
      containers:
      - name: pod-data-download-sso
        image: ubuntu:20.04
        command: ["/bin/sh","-c"]
        args:
        - cp -r /solaiman/* /data/
        volumeMounts:
        - mountPath: /solaiman
          name: source
        - mountPath: /data
          name: destination 
        resources:
            limits:
              memory: 10Gi
              cpu: "4"
            requests:
              memory: 10Gi
              cpu: "4"     
      volumes:
      - name: source
        persistentVolumeClaim:
          claimName: cloudcomp-data
      - name: destination
        persistentVolumeClaim:
          claimName: cloudcomp-solaiman
      restartPolicy: Never      
  backoffLimit: 0
